# Use a dedicated RPC in production. Helius free tier is enough for early development.
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# Optional. If omitted, @solana/web3.js derives wss:// from SOLANA_RPC_URL.
SOLANA_WS_URL=
